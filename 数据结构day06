稀疏矩阵加法
	三元组保存法 行优先
	同行同列，无效值相同才能加
	
树
	概念
	
	
孩子兄弟表示 

二叉树的存储
	度为2，
	顺序存储：按数组下标：完全二叉树不影响/普通二叉树要留空位
	二叉树的遍历：--->访问--->前序，后序，中序，（层序？）
	void PreOrder(pNode* pRoot)//前序遍历
	{
		if(pRoot)
		{
			cout<<pRoot->data<<" ";
			PreOrder(pRoot->left);
			preOrder(pRoot->right);
		}
	}
	
	创建二叉树？？？
